# EX1_Yonatan

A minimal C program that prints a friendly greeting in Hebrew. The program tries to set
your environment locale before printing so the Hebrew text renders correctly.

## Build and run

```bash
gcc -Wall -Wextra -o hello main.c
./hello
```
